# §9 Definition of Done audit — 2026-07-12

**Process contract:** `codex-goal-v2.md` §9  
**Audit HEAD:** `c7049069` (pre-ticket-10 docs commit; same product tree as final gates)  
**Final gate log root:** `build/s4d383-dod-audit/`  
**Local program:** tickets 01–09 **complete** under `.scratch/easywatermark-kmp-cmp-migration/`

**Overall §9:** **NOT MET** (honest). Program is **graduation-proposal ready**, not “migration complete / PR merge-ready.”

Legend: **PASS** · **PARTIAL** · **FAIL** · **N/A**

---

## Checklist

### 1. `:app` assembleDebug + assembleRelease; non-strict unit; strict goldens

| Item | Status | Evidence |
|------|--------|----------|
| assembleDebug | **PASS** | `build/s4d383-dod-audit/01-app-shared.log` EXIT 0 |
| assembleRelease | **PASS** | same (R8 + packageRelease) |
| non-strict `testDebugUnitTest` | **PASS** | **53/0** (suite XMLs under `app/build/test-results/testDebugUnitTest`) |
| strict goldens (`WATERMARK_GOLDEN_STRICT=true`) | **PARTIAL** | Policy: **local / pinned-env only** (S4d-171/172; not PR CI). **Not re-run** in this audit session. Prior program accepted non-strict + structural nets; rebaseline needs owner if strict fails on host. |

### 2. `:shared` all 4 targets compile; commonTest / desktopTest / iosSimulatorArm64Test

| Item | Status | Evidence |
|------|--------|----------|
| compile android + desktop + iosArm64 + iosSimulatorArm64 | **PASS** | `01-app-shared.log` EXIT 0 |
| desktopTest | **PASS** | **132/0** |
| commonTest (as part of multiplatform nets) | **PASS** (via desktop/shared suites historically + compile) | B3/A5 closeout also green |
| iosSimulatorArm64Test **RUN** | **PARTIAL** | Compile green this audit; **full native test RUN** last proven in program history (e.g. S4d-55 era + ongoing suite). **Not re-executed** in `s4d383-dod-audit` (time/sim load). Recommend owner optional re-run before public ship. |

### 3. Desktop run / headless / createDistributable / packaging CI

| Item | Status | Evidence |
|------|--------|----------|
| `:desktopApp:run` headless | **PASS** | `02-desktop-headless.log` EXIT 0 |
| `createDistributable` app image | **PASS** | Corretto 17 · `05-createDistributable.log` EXIT 0 · `desktopApp/build/compose/binaries/main/app/EasyWatermark.app` |
| Desktop packaging CI | **PASS** (historical green on PR) | `desktop_packaging.yml` last runs **success** (2026-06-29/30 on #358). Not re-triggered this session. |
| Unsigned only | **Noted** | No DMG/notarization claim (ADR-0013 packaging posture). |

### 4. iosApp build / install / run; XCUITest

| Item | Status | Evidence |
|------|--------|----------|
| Build/install/run + Shared.framework | **PASS** (prior) | A5a / S4d-55+ program evidence; not re-installed this audit |
| Full `iosAppUITests` | **PASS** (prior) | `build/s4d383-a5a-final/35-full-xcodebuild.log` + `ios-full.xcresult` → **19/0** TEST SUCCEEDED |
| PHPicker grid-cell selection | **N/A exception** | Toolchain residual (documented E14); fixture seam remains valid |

### 5. Shared CMP route of record; native UI only at listed edges

| Item | Status | Evidence |
|------|--------|----------|
| Android editor/launch/gallery/about CMP | **PASS** | ComposeMainActivity + shared shells |
| iOS editor CMP host | **PASS** | `IosEditorScreenHost` / ticket 01 |
| Desktop editor CMP | **PASS** | `DesktopWindow` + `EditorScreenShell` |
| Edge list | **PASS** | `docs/parity/v2.10.0/alignment/ios-desktop-exception-registry.md` E01–E16 |

### 6. Data layer commonMain + documented edges; persisted bytes

| Item | Status | Evidence |
|------|--------|----------|
| Models/repos/DataStore/Room/use-cases in commonMain | **PASS** | AGENTS.md architecture; S4d-74..98 lane |
| Platform store/DB factories | **PASS** | Plain per-platform create (no expect/actual DataStore factory) |
| Persisted bytes sacred | **PASS** (by closed policy) | No migration/rebaseline authorized in 07–09; goldens not rebaselined |

### 7. Android 1:1 parity archive + owner sign-off; iOS/Desktop exceptions

| Item | Status | Evidence |
|------|--------|----------|
| Archive scaffold + en/dark captures | **PASS** | `docs/parity/v2.10.0/` |
| Full matrix (all states × en/zh × light/dark + recordings) | **PARTIAL** | Core launch/gallery/editor/export **owner-signed** (tickets 07/08). Not every inventory row photographed for all locales/themes. |
| Owner screen sign-off | **PASS** (scoped) | 07 launch+gallery; 08 editor+export — owner explicit approval |
| iOS/Desktop alignment exceptions | **PASS** | Ticket 09 registry |

### 8. Docs / ADRs / local tickets / process

| Item | Status | Evidence |
|------|--------|----------|
| Local tickets 01–09 complete | **PASS** | `.scratch/easywatermark-kmp-cmp-migration/` |
| Process = codex-goal-v2; ACSP retired | **PASS** | Contract file |
| AGENTS.md / CONTEXT / ADRs | **PARTIAL** | Architecture rules still valid; **codex-goal-v2 §11 “current truth” is stale** (still says Phase A NOT complete / frontier 01). Recommend follow-up doc refresh after owner accepts this audit. |
| Public GitHub migration issues | **N/A** | Ops backend is local only |

### 9. PR #358 graduation proposal to owner (not auto-merge)

| Item | Status | Evidence |
|------|--------|----------|
| Proposal document | **PASS** | This audit + `graduation-proposal-pr358.md` |
| Auto-merge | **Forbidden** | Owner decision only |

---

## Residual gap list (blocks claiming “migration complete”)

1. **Strict watermark goldens** not re-run on a pinned host this audit.  
2. **`iosSimulatorArm64Test` full RUN** not in final audit log (compile only).  
3. **Parity archive** is owner-signed for core en/dark flows, not exhaustive locale/theme/recording matrix.  
4. **Process doc §11** in `codex-goal-v2.md` outdated vs tickets 01–09 complete.  
5. **Desktop packaging** = unsigned app image + CI green historically; not notarized public ship.  
6. **Renderer split** (Android native vs Desktop/iOS Skiko) remains by closed ADR — intentional, not a bug.

---

## Verdict line

**§9 overall: NOT MET.**  
**Program status: Phase A closed; Phase B core Android screens owner-signed; iOS/Desktop exceptions registered; ready for owner graduation decision on PR #358 — not for unattended merge.**
