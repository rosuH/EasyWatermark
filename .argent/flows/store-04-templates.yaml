steps:
  - launch: me.rosuh.easywatermark.ios
  - wait: 2000
  - echo: Seed photos, then open the template list
  - tap:
      id: store-seed-templates
  - wait: 7000
  - await:
      idle: true
      timeout: 15000
